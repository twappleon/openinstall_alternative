# 根目录问题诊断

## 🔍 问题确认

**Vercel 返回 404，最可能的原因是：Root Directory 配置错误**

### 当前情况

- **Git 仓库**: `openinstall_alternative`
- **项目位置**: `openinstall-web/` (子目录)
- **Vercel 项目**: `openinstall-web`
- **问题**: Vercel 可能在仓库根目录查找文件，而不是 `openinstall-web` 子目录

## ✅ 解决方案

### 必须在 Vercel Dashboard 中设置

1. **访问 Vercel Dashboard**：
   - https://vercel.com
   - 登录账户
   - 找到 `openinstall-web` 项目

2. **设置 Root Directory**：
   - 进入 **Settings** → **General**
   - 找到 **Root Directory** 设置
   - **必须设置为**: `openinstall-web`
   - 点击 "Save"

3. **重新部署**：
   - 进入 **Deployments** 标签
   - 点击 "Redeploy"
   - 等待部署完成

## 📋 验证步骤

设置 Root Directory 并重新部署后：

1. **等待 1-2 分钟**

2. **访问网站**：
   ```
   https://openinstall-web.vercel.app/
   ```
   应该能正常显示。

3. **访问带参数**：
   ```
   https://openinstall-web.vercel.app/?inviteCode=ABC123
   ```
   应该能正常显示页面。

## 🔧 如果设置后仍然 404

可能需要：

1. **删除项目并重新导入**：
   - 删除当前项目
   - 重新导入 GitHub 仓库
   - **重要**：在导入时设置 Root Directory 为 `openinstall-web`

2. **或使用其他部署方式**：
   - Netlify（更简单）
   - Cloudflare Pages
   - 直接部署 `openinstall-web` 目录

## 📝 当前文件状态

所有文件都存在且正确：
- ✅ `index.html` - 存在
- ✅ `openinstall.js` - 存在
- ✅ `downloads/app.apk` - 存在 (18MB)
- ✅ `vercel.json` - 配置正确

**问题不在文件，而在 Vercel 的 Root Directory 配置！**

---

**必须在 Vercel Dashboard 中设置 Root Directory 为 `openinstall-web`！**

