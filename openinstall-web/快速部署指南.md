# 快速部署指南

## 🎯 目标

将修复了 MainActivity 崩溃问题的新 APK 部署到 Vercel。

## ✅ 当前状态

- ✅ APK 已修复（包含 MainActivity.kt）
- ✅ APK 文件已复制到 `downloads/app.apk` (17MB)
- ⏳ 需要重新部署到 Vercel

## 🚀 部署步骤

### 选项 1: 使用部署脚本

```bash
cd openinstall-web
./deploy.sh
```

### 选项 2: 手动部署

```bash
cd openinstall-web
npx vercel --prod
```

如果遇到权限问题：
```bash
npx vercel login
npx vercel --prod
```

### 选项 3: 通过 Vercel 网站

1. 访问 https://vercel.com
2. 登录账户
3. 找到 `openinstall-web` 项目
4. 进入项目设置
5. 点击 "Redeploy" 或手动上传文件

### 选项 4: 通过 Git（如果已连接）

```bash
cd openinstall-web
git add downloads/app.apk
git commit -m "Update APK: Fix MainActivity crash"
git push
```

## 🔍 验证

部署完成后：

1. **访问网页**：https://openinstall-web.vercel.app/?inviteCode=ABC123
2. **下载 APK**：https://openinstall-web.vercel.app/downloads/app.apk
3. **安装测试**：
   - 完全卸载旧版本
   - 安装新版本
   - 应该不再闪退

## 📋 修复内容

本次 APK 修复：
- ✅ 创建了 `MainActivity.kt` 文件
- ✅ 修复了应用启动崩溃问题
- ✅ APK 大小：17MB
- ✅ 构建时间：2024-11-26 11:04

## ⚠️ 重要提示

1. **完全卸载旧版本**：
   ```bash
   adb uninstall com.openinstall.flutter.example
   ```

2. **清除缓存**（可选）：
   ```bash
   adb shell pm clear com.openinstall.flutter.example
   ```

3. **重新安装**新版本 APK

## 🐛 如果仍然闪退

请运行日志收集脚本：

```bash
cd openinstall-flutter/example
./get_crash_log.sh
```

然后启动应用，查看具体的错误信息。

