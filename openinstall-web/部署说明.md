# Vercel 部署说明

## 📦 当前状态

APK 文件已更新（包含 MainActivity.kt 修复）：
- 位置：`downloads/app.apk`
- 大小：17MB
- 时间：2024-11-26 11:04

## 🚀 部署方法

### 方法 1: 使用 Vercel CLI（推荐）

```bash
cd openinstall-web
npx vercel --prod
```

如果遇到权限问题，可能需要：
1. 登录 Vercel：`npx vercel login`
2. 或者使用正确的账户

### 方法 2: 通过 Vercel 网站

1. 访问 [vercel.com](https://vercel.com)
2. 登录你的账户
3. 找到 `openinstall-web` 项目
4. 点击 "Redeploy" 或上传新的文件

### 方法 3: 通过 Git 推送

如果项目已连接到 Git 仓库：

```bash
cd openinstall-web
git add downloads/app.apk
git commit -m "Update APK with MainActivity fix"
git push
```

Vercel 会自动部署。

## ✅ 验证部署

部署后，访问：
- 网页：https://openinstall-web.vercel.app/?inviteCode=ABC123
- 直接下载：https://openinstall-web.vercel.app/downloads/app.apk

下载 APK 并测试，应该不再闪退。

## 🔍 如果仍然闪退

如果更新后仍然闪退，请：

1. **确认下载的是新版本**：
   - 检查 APK 文件大小（应该是 17MB）
   - 检查文件时间戳

2. **完全卸载旧版本**：
   ```bash
   adb uninstall com.openinstall.flutter.example
   ```

3. **重新安装**：
   - 下载新的 APK
   - 安装并测试

4. **收集日志**：
   ```bash
   cd openinstall-flutter/example
   ./get_crash_log.sh
   ```

## 📝 修复内容

本次修复：
- ✅ 创建了 MainActivity.kt 文件
- ✅ 修复了应用启动崩溃问题
- ✅ 重新构建了 APK

