# 恢复配置说明

## 🔍 问题分析

原本可以正常打开，突然变成 404，原因是：

**最近的配置更改移除了 `builds` 配置**，导致 Vercel 无法正确识别静态文件。

## ✅ 已恢复

恢复了之前工作的配置（基于提交 `3a5516d`）：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/downloads/(.*)",
      "dest": "/downloads/$1",
      "headers": {
        "Content-Type": "application/vnd.android.package-archive"
      }
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

## 🔑 关键配置

1. **`builds`** - 明确告诉 Vercel 使用静态文件服务
2. **`routes`** - 配置路由规则
3. **APK 下载** - 设置正确的 Content-Type

## 🚀 已重新部署

- ✅ 已恢复工作配置
- ✅ 已提交并推送（等待自动部署）

## ✅ 验证

等待 1-2 分钟后：

1. **访问主页**：
   ```
   https://openinstall-web.vercel.app/?inviteCode=ABC123
   ```
   应该能正常显示页面。

2. **下载 APK**：
   ```
   https://openinstall-web.vercel.app/downloads/app.apk
   ```
   应该能正常下载。

---

**配置已恢复到之前工作的版本，等待 Vercel 自动部署即可！**

