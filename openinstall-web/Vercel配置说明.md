# Vercel 配置说明

## ⚠️ 当前问题

访问 https://openinstall-web.vercel.app/ 返回 404。

## 🔍 可能的原因

1. **项目根目录配置错误** - Vercel 可能配置了错误的根目录
2. **文件路径问题** - 文件可能不在正确的位置
3. **部署配置问题** - 需要检查 Vercel Dashboard 设置

## ✅ 已尝试的修复

1. ✅ 简化了 vercel.json 配置
2. ✅ 移除了复杂的路由配置
3. ✅ 使用最小化配置

## 🛠️ 需要在 Vercel Dashboard 检查

### 1. 检查根目录设置

1. 访问 https://vercel.com
2. 登录并找到 `openinstall-web` 项目
3. 进入 **Settings** → **General**
4. 检查 **Root Directory** 设置：
   - 如果仓库根目录是 `openinstall_alternative`
   - 需要设置为：`openinstall-web`
   - 或者保持为空（如果项目在仓库根目录）

### 2. 检查部署配置

1. 进入 **Settings** → **Build & Development Settings**
2. 检查：
   - **Framework Preset**: 应该是 "Other" 或 "Static Site"
   - **Build Command**: 留空
   - **Output Directory**: `.` 或留空
   - **Install Command**: 留空

### 3. 手动触发重新部署

1. 进入 **Deployments** 标签
2. 找到最新的部署
3. 点击 "..." → "Redeploy"
4. 或点击 "Redeploy" 按钮

## 📋 当前文件结构

```
openinstall-web/
├── index.html          ✅ 存在
├── openinstall.js      ✅ 存在
├── downloads/
│   └── app.apk         ✅ 存在 (18MB)
└── vercel.json         ✅ 最小配置
```

## 🔧 如果仍然 404

可能需要：

1. **删除并重新创建项目**：
   - 在 Vercel Dashboard 中删除项目
   - 重新导入 GitHub 仓库
   - **重要**：设置 Root Directory 为 `openinstall-web`

2. **或使用其他部署方式**：
   - Netlify（更简单的静态网站部署）
   - Cloudflare Pages
   - GitHub Pages

## 📝 当前配置

`vercel.json`:
```json
{
  "version": 2,
  "public": true
}
```

这是最小配置，让 Vercel 自动检测静态文件。

---

**请检查 Vercel Dashboard 中的 Root Directory 设置！**

