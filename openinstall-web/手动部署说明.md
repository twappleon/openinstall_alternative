# 手动部署说明

## 🔐 权限问题

由于 Vercel 权限限制，需要通过以下方式之一部署：

## 方法 1: 通过 Vercel 网站（推荐）

1. **访问 Vercel 网站**：
   - 打开 https://vercel.com
   - 登录你的账户

2. **找到项目**：
   - 在 Dashboard 中找到 `openinstall-web` 项目
   - 点击进入项目

3. **重新部署**：
   - 点击 "Deployments" 标签
   - 找到最新的部署
   - 点击 "..." 菜单
   - 选择 "Redeploy"
   - 或者点击 "Settings" → "Git" → 如果有连接 Git，推送代码会自动部署

## 方法 2: 通过 Git 推送（如果已连接）

如果项目已连接到 Git 仓库（GitHub/GitLab/Bitbucket）：

```bash
cd openinstall-web
git add downloads/app.apk
git commit -m "Update APK: Fix MainActivity crash issue"
git push
```

Vercel 会自动检测到推送并重新部署。

## 方法 3: 手动上传文件

1. **访问 Vercel Dashboard**
2. **进入项目设置**
3. **使用 Vercel CLI 但先登录**：
   ```bash
   # 在浏览器中完成登录
   npx vercel login
   
   # 然后部署
   npx vercel --prod
   ```

## 方法 4: 使用 Vercel Web UI 上传

1. 访问 https://vercel.com/new
2. 选择 "Import Git Repository" 或 "Deploy"
3. 上传 `openinstall-web` 目录

## ✅ 验证部署

部署完成后：

1. **访问网页**：https://openinstall-web.vercel.app/?inviteCode=ABC123
2. **检查 APK**：
   - 直接下载：https://openinstall-web.vercel.app/downloads/app.apk
   - 文件大小应该是 17MB
   - MD5：`c639709afbd103aef0914b58a9089fef`

## 📝 当前 APK 信息

- **文件位置**：`openinstall-web/downloads/app.apk`
- **文件大小**：17MB
- **修复内容**：已添加 MainActivity.kt，解决启动崩溃问题
- **构建时间**：2024-11-26 11:04

## 🎯 部署后测试

1. **完全卸载旧版本**：
   ```bash
   adb uninstall com.openinstall.flutter.example
   ```

2. **下载新版本 APK**

3. **安装并测试**：应用应该能正常启动，不再闪退

