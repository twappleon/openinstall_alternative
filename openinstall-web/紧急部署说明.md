# ⚠️ 紧急部署说明

## ❌ 当前问题

**Vercel 上的 APK 仍然是旧版本，导致应用闪退！**

- **远程 APK**（旧版本）：MD5 `b49313164aa031a1df5316e8831d699f` - ❌ 会闪退
- **本地 APK**（新版本）：MD5 `bda9be7b7afbd83c9c3e97dd02a3ea2b` - ✅ 已修复

## ✅ 新版本修复内容

1. ✅ **MainActivity.kt** - 修复启动崩溃
2. ✅ **参数验证功能** - 显示获取到的参数
3. ✅ **错误处理** - 完善的错误处理机制

## 🚀 立即部署

### 方法 1: Vercel 网站（最快，推荐）

1. **打开浏览器**：https://vercel.com
2. **登录账户**
3. **找到项目**：`openinstall-web`
4. **重新部署**：
   - 点击项目
   - 点击 "Deployments"
   - 找到最新部署
   - 点击 "..." → "Redeploy"
   - 或直接点击 "Redeploy" 按钮

### 方法 2: CLI 部署

```bash
cd openinstall-web

# 如果还没登录，先登录
npx vercel login

# 部署
npx vercel --prod
```

## ✅ 部署后验证

部署完成后（1-2分钟），验证新版本：

```bash
# 下载并检查 MD5
curl -o /tmp/verify.apk https://openinstall-web.vercel.app/downloads/app.apk
md5 /tmp/verify.apk

# 应该输出: bda9be7b7afbd83c9c3e97dd02a3ea2b
```

如果 MD5 匹配，说明部署成功！

## 📱 测试步骤

1. **完全卸载旧版本**：
   ```bash
   adb uninstall com.openinstall.flutter.example
   ```

2. **下载新版本**：
   - 访问：https://openinstall-web.vercel.app/?inviteCode=ABC123
   - 点击"立即下载"

3. **安装并测试**：
   - 安装新 APK
   - 打开应用
   - **应该不再闪退！**
   - 会显示参数获取界面

## 🔍 如果仍然闪退

1. **确认下载的是新版本**：
   - 检查文件大小（应该是 18MB）
   - 检查 MD5

2. **清除缓存**：
   ```bash
   adb shell pm clear com.openinstall.flutter.example
   ```

3. **完全卸载重装**：
   ```bash
   adb uninstall com.openinstall.flutter.example
   # 然后重新安装
   ```

4. **收集日志**：
   ```bash
   cd openinstall-flutter/example
   ./get_crash_log.sh
   ```

## 📝 当前文件状态

- ✅ **本地 APK**：`openinstall-web/downloads/app.apk` (18MB)
- ✅ **包含修复**：MainActivity.kt + 参数验证
- ⏳ **等待部署**：需要上传到 Vercel

---

**请立即部署新版本，部署后应用将不再闪退！**

