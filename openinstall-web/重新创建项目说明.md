# 重新创建 Vercel 项目说明

## 🔍 问题分析

Vercel Dashboard 中没有 Root Directory 设置，可能是因为：
1. 项目是从仓库根目录创建的
2. 需要重新创建项目，直接指向 `openinstall-web` 目录

## ✅ 解决方案：重新创建项目

### 方法 1: 在 Vercel Dashboard 中重新导入

1. **访问 Vercel Dashboard**：
   - https://vercel.com
   - 登录账户

2. **删除当前项目**（可选）：
   - 找到 `openinstall-web` 项目
   - Settings → Delete Project

3. **重新导入项目**：
   - 点击 "Add New..." → "Project"
   - 选择 GitHub 仓库：`twappleon/openinstall_alternative`
   - **重要**：在导入配置中：
     - **Root Directory**: 设置为 `openinstall-web`
     - **Framework Preset**: 选择 "Other" 或留空
     - **Build Command**: 留空
     - **Output Directory**: `.` 或留空

4. **部署**：
   - 点击 "Deploy"
   - 等待部署完成

### 方法 2: 使用 Vercel CLI 重新链接

如果当前项目有问题，可以重新链接：

```bash
cd openinstall-web

# 删除旧的链接
rm -rf .vercel

# 重新链接（会提示选择或创建项目）
npx vercel link

# 部署
npx vercel --prod
```

### 方法 3: 从 openinstall-web 目录直接部署

```bash
cd openinstall-web

# 如果还没链接，先链接
npx vercel link

# 部署
npx vercel --prod
```

## 📋 关键点

**最重要**：确保 Vercel 项目指向 `openinstall-web` 目录，而不是仓库根目录。

如果项目是从仓库根目录创建的，Vercel 会在根目录查找 `index.html`，但文件实际在 `openinstall-web/` 子目录中，所以返回 404。

## 🔍 验证

部署后，访问：
- https://openinstall-web.vercel.app/
- https://openinstall-web.vercel.app/?inviteCode=ABC123

应该能正常显示页面。

---

**建议：重新创建项目，确保 Root Directory 指向 `openinstall-web`！**

