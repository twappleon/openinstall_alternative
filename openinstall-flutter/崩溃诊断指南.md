# 崩溃诊断指南

## 🔍 收集崩溃日志

### 方法 1: 使用脚本（推荐）

```bash
cd openinstall-flutter/example
./get_crash_log.sh
```

然后启动应用，脚本会自动收集日志。

### 方法 2: 手动收集

```bash
# 设置 adb 路径
export PATH=$PATH:$HOME/Library/Android/sdk/platform-tools

# 清除旧日志
adb logcat -c

# 收集日志（启动应用后）
adb logcat > crash_log.txt
```

### 方法 3: 使用 Flutter 日志

```bash
cd openinstall-flutter/example
flutter logs
```

## 🔎 关键日志信息

查找以下关键词：

1. **FATAL EXCEPTION** - 致命异常
2. **AndroidRuntime** - Android 运行时错误
3. **Exception** - 异常信息
4. **Error** - 错误信息
5. **flutter** - Flutter 相关错误
6. **openinstall** - OpenInstall 相关错误

## 📋 常见崩溃原因

### 1. 原生插件问题

**症状：** 日志显示 `MethodChannel` 或插件相关错误

**检查：**
- `OpenInstallPlugin.kt` 是否正确注册
- 插件依赖是否正确

### 2. 资源文件缺失

**症状：** 日志显示资源找不到

**检查：**
- `styles.xml` 是否存在
- `ic_launcher` 图标是否存在
- `launch_background.xml` 是否存在

### 3. 权限问题

**症状：** 日志显示权限被拒绝

**检查：**
- `AndroidManifest.xml` 中的权限配置

### 4. 依赖冲突

**症状：** 日志显示类冲突或版本不兼容

**解决：**
```bash
flutter clean
flutter pub get
flutter build apk
```

## 🛠️ 测试步骤

### 步骤 1: 测试最小版本

使用 `main_minimal.dart`：

```bash
cd openinstall-flutter/example
cp lib/main.dart lib/main_backup.dart
cp lib/main_minimal.dart lib/main.dart
flutter build apk
```

如果最小版本能启动，说明问题在业务逻辑中。

### 步骤 2: 逐步添加功能

1. 先测试基础 UI
2. 再添加状态管理
3. 最后添加 SDK 功能

### 步骤 3: 检查构建配置

```bash
flutter clean
flutter pub get
flutter build apk --verbose
```

查看详细构建日志。

## 📝 需要提供的信息

如果问题仍然存在，请提供：

1. **完整的崩溃日志**（使用 `get_crash_log.sh`）
2. **设备信息**：
   - 设备型号
   - Android 版本
3. **构建信息**：
   - Flutter 版本
   - 构建命令
4. **崩溃时机**：
   - 启动时立即崩溃
   - 运行一段时间后崩溃
   - 特定操作后崩溃

## 🔗 相关文件

- `example/lib/main.dart` - 当前主文件
- `example/lib/main_minimal.dart` - 最小测试版本
- `example/lib/main_safe.dart` - 安全测试版本
- `example/get_crash_log.sh` - 日志收集脚本
- `android/src/main/kotlin/com/openinstall/flutter/OpenInstallPlugin.kt` - 原生插件

