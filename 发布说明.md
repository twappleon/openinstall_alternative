# OpenInstall 发布说明

## 📋 发布内容

本次发布包括：
1. **OpenInstall-flutter** - Flutter SDK 和示例应用
2. **OpenInstall-web** - Web 落地页

## 🚀 发布步骤

### 方法 1: 使用发布脚本（推荐）

```bash
cd /Users/leon/Downloads/example
./发布脚本.sh
```

脚本会自动执行：
1. ✅ 检查 Flutter 环境
2. ✅ 构建 Flutter APK
3. ✅ 复制 APK 到 web 目录
4. ✅ 提交代码到 Git
5. ✅ 部署到 Vercel

### 方法 2: 手动发布

#### 1. 构建 Flutter APK

```bash
cd openinstall-flutter
flutter pub get
cd example
flutter build apk --release
```

#### 2. 复制 APK 到 web 目录

```bash
cd ../../..
cp openinstall-flutter/example/build/app/outputs/flutter-apk/app-release.apk \
   openinstall-web/downloads/app.apk
```

#### 3. 提交代码到 Git

```bash
git add -A
git commit -m "发布 OpenInstall-flutter 和 OpenInstall-web"
git push origin main
```

#### 4. 部署到 Vercel

```bash
cd openinstall-web
npx vercel --prod --yes
```

## 📋 本次更新内容

### 后端 (openinstall-backend)

1. **字段值标准化**
   - 标准化 `platform`: `"Win32"` → `"windows"`, `"android"` → `"android"`
   - 标准化 `timezone`: `"CST"` → `"Asia/Shanghai"` (IANA 格式)
   - 标准化 `userAgent`: 提取关键信息（操作系统和版本）
   - 移除 Canvas 指纹（Web 端特有，移动端没有）

2. **统一 fingerprintId 计算**
   - 后端统一计算 fingerprintId
   - 确保保存和匹配时使用相同的算法

### Web 端 (openinstall-web)

1. **MD5 算法**
   - 使用 `crypto-js` 计算 MD5
   - 与后端算法保持一致

2. **部署配置**
   - 更新 `vercel.json`
   - 确保 APK 文件正确部署

### Flutter 端 (openinstall-flutter)

1. **MD5 算法**
   - 使用 `crypto` 包计算 MD5
   - 与后端算法保持一致

2. **依赖更新**
   - 添加 `crypto: ^3.0.3` 依赖

## 🌐 访问地址

### Web 端

- **主页**: https://openinstall-web.vercel.app/
- **测试链接**: https://openinstall-web.vercel.app/?inviteCode=ABC123
- **APK 下载**: https://openinstall-web.vercel.app/downloads/app.apk

### 后端 API

- **API 地址**: https://openinstall-backend.zeabur.app/api
- **健康检查**: https://openinstall-backend.zeabur.app/api/tracking/health

## 🧪 测试验证

### 1. Web 端测试

1. 访问测试链接：https://openinstall-web.vercel.app/?inviteCode=ABC123
2. 打开浏览器控制台（F12）
3. 检查 `fingerprintId` 是否为 32 位 MD5 字符串
4. 查看后端日志，确认数据已保存

### 2. Flutter 端测试

1. 下载并安装 APK：https://openinstall-web.vercel.app/downloads/app.apk
2. 打开应用
3. 检查是否能获取到追踪参数（inviteCode 等）
4. 查看后端日志，确认 fingerprintId 匹配成功

### 3. 一致性验证

1. 在同一设备上：
   - Web 端访问并保存数据
   - Flutter 端打开应用并匹配数据
2. 检查后端日志：
   - 保存的 fingerprintId
   - 匹配的 fingerprintId
   - 应该一致（如果字段值标准化正确）

## ⚠️ 注意事项

1. **Flutter 环境**
   - 确保已安装 Flutter SDK
   - 确保 Android 开发环境已配置

2. **Vercel 部署**
   - 确保已登录 Vercel CLI
   - 确保项目已链接到 Vercel

3. **后端部署**
   - 后端需要重新编译和部署
   - 确保字段值标准化功能已生效

## 📝 相关文档

- `fingerprintId问题解决方案.md` - 完整的问题分析和解决方案
- `fingerprintId差异分析.md` - 字段值差异详细分析
- `fingerprintId统一修复说明.md` - 统一修复说明

---

**发布完成后，请测试验证所有功能是否正常！** ✅

